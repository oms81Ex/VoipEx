# TURN 서버 설정
listening-port=3478
tls-listening-port=5349

# 외부 IP (실제 IP로 변경 필요)
# external-ip=YOUR_PUBLIC_IP/YOUR_PRIVATE_IP

# 인증
lt-cred-mech
use-auth-secret
static-auth-secret=your-turn-secret-key

# 포트 범위
min-port=49152
max-port=65535

# 로깅
log-file=stdout
verbose

# 릴레이
no-multicast-peers
